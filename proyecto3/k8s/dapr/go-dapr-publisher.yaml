apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-dapr-publisher
  namespace: proyecto3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-dapr-publisher
  template:
    metadata:
      labels:
        app: go-dapr-publisher
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "go-dapr-publisher"
        dapr.io/app-port: "8082"
    spec:
      imagePullSecrets:
        - name: zot-login
      containers:
        - name: go-dapr-publisher
          image: 136.113.143.168:5000/proyecto3/go-dapr-publisher:dev
          imagePullPolicy: Always
          ports:
            - containerPort: 8082
          # Optimización de Rendimiento 
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: go-dapr-publisher
  namespace: proyecto3
spec:
  selector:
    app: go-dapr-publisher
  ports:
    - port: 8082
      targetPort: 8082

apiVersion: v1
kind: Service
metadata:
  name: grafana-nodeport
  namespace: proyecto3
spec:
  type: NodePort
  selector:
    kubevirt.io/vm: grafana-vm
  ports:
    - name: grafana
      protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 32000
